# 青全交のしおり in 仙台（sendai-guide）— Claude Code 起動時ガイド

> 毎セッション最初に読むブートストラップ。

## これは何

「中小企業家同友会 **青年経営者全国交流会（青全交）in 仙台**」（**2026/10/1(木)〜2(金)**）に参加する経営者向けのしおりWebサイト。幹事が情報をまとめて共有する用途で、**スマホ閲覧が主**。

⚠️ **読者規模は当初想定から変わっている（2026-08-07 更新）**。当初は「名古屋第9支部 約25名」だったが、実際は **数百人（200人前後）** が、**開催前から繰り返し** 閲覧する見込み。読者は **愛知の青全交参加者だけ**（2026-09-19 宇川さん確認）。なので「自地区」のような愛知目線の言い方はそのままでよい（丁寧語で書くことは別の話で、そちらは守る）。セントレア発の飛行機時刻・名鉄の案内・名古屋との街比較はこのままで正しい。「25人向けだから雑でよい」という前提で判断しないこと。

- **公開URL：https://sendai.apppro-web.com/**（参加者に共有済みの公開サイト）
- 素の静的HTML。フレームワークなし・外部API/DBなし。
- ⚠️ **呼称は「青全交のしおり in 仙台」**（旧「旅のしおり」から変更済み）。「青全交」は公式パンフレットでも使われる正式略称。**古い資料に残る「旅のしおり」表記に引きずられないこと。**
- **期限のあるプロジェクト**。開催（2026/10/1）を過ぎたら役目を終える。申込締切は 9/16。

## デプロイ

- 配信：Cloudflare **Workers** プロジェクト `sendai-guide`（Static Assets）。**Pagesではない。**
- **`main` に push → Workers Builds が自動デプロイ**（数十秒）。
- `wrangler.jsonc`（`assets.directory = "."`）はリポジトリのルートにある。元はCloudflareが生成した `cloudflare/workers-autoconfig` ブランチにしか無かったが、mainに取り込み済みでビルドは正常。
- 運用ルール：**`main` は参加者が見る公開サイト**。文言・情報の微修正は main 直行可。レイアウトを触る改修は `preview/<topic>` → プレビューURLで確認 → main へ。
  - プレビューURL：`preview-<branch>-sendai-guide.apple-checker.workers.dev`（check-run の Preview Alias URL が正）
- ⚠️ **反映直後はエッジ／ブラウザのキャッシュで旧版が見えることがある。** 直したのに変わらない時は **Ctrl+Shift+R**。「デプロイが失敗した」と誤診しないこと。

## ファイル構成

- **`index.html`（約90KB）** … 本体。1ファイルにCSS/JS/データが全部入り。セクションは `#schedule` `#gourmet` `#access` `#hotel` `#area` の5つ。データはすべてHTMLにベタ書きなので、改修は該当箇所を直接編集する。
- **`sendai-report.html`（`/sendai-report`）** … 支部への報告資料（QRコード入り・別ページ）
- **`pamphlet.pdf`（`/pamphlet.pdf`）** … イベント公式パンフレット
- `qa/` … QA生成物。`.gitignore` で除外済み

## 改修時の方針（守ること）

- 🔴 **文言は、不特定多数の読み手に向けた丁寧な案内文で書く**（200人前後が見る公開サイト。幹事ひとりのメモではない）。「自分の〜」のような作り手・一人称目線の言い方をしない→「ご自身の〜をご確認ください」。一部の人にだけ当てはまる話は条件を書く（例: 懇親会は申込者のみ→「懇親会にもご参加の方は」）。2026-09-19に指摘されて直した。
- 🔴 **ブロックを丸ごと移動したら、必ず「実際に表示される文字」を見る**。タグの数が合っていても断片は残る（正規表現の一致長で slice して行頭インデント分ずれ、`div>` が裸のテキストとして本番に出た実例あり）。ブラウザで開いて本文を読むか、テキストノードを走査して `<`/`div>`/`class=` 等が混じっていないか確認する。
- 🔴 **内容を直したら、最終更新バナーの日付と「更新履歴」に1行足す**（`index.html` の `.updated-banner` 内 `.ub-log`）。200人が開催前から**繰り返し**見に来るので、差分が追えないと毎回全部読み直させることになる。直近3件程度に保つ。見た目だけの微修正なら足さなくてよい。
- 🔴 **分科会の会場（建物・階・部屋）は、各分科会カードの `.bk-venue`（📍の行）にだけ載せる**。別枠の一覧は作らない。経緯＝9/18に番号順の一覧を作り、カードにも📍を写したら重複と指摘（9/19に一覧だけ残す）→ さらに9/20に「カード側へまとめたい」となり、**一覧を廃止してカードに集約**。3建物の内訳や移動の注意は、カード群の上の `.bunkakai-note` に置く。出典は主催の「分科会会場一覧」PDF。
- **配色は2系統を混ぜない**。明るい地の上＝`--gold` `#c4a265`／ネイビー地の上＝`--gold-dark` `#e0c68e`。ネイビー地の文字は半透明にせず `--on-dark-2` `--on-dark-3` を使う（半透明だと明るい場所で読めなくなる）。**明るい地の上で金文字は使わない**（読めない。文字は `--ink`、金は線やアクセントに回す）。
- **新しいクラス名を足す前に `index.html` を grep する。** 1ファイルにCSSが全部入りなので同名が既にあると黙って衝突する（2026-09-03実例: `.map-legend` は周辺セクションで絶対配置に使われており、グルメ欄の凡例に同名を付けたらカード外に飛んだ→ `mymap-` 接頭辞に改名して解消）。
- **列指定などのレイアウトを HTML の `style` 属性に直書きしない**。インラインが勝ってスマホ用のメディアクエリが効かなくなる（宿泊候補が3列のまま潰れた実例あり）。
- **現状のデザインを大きく崩さない**（ユーザー明言）。配色変数は `:root` で一元管理。Zen Old Mincho 明朝＋ゴールド×クリーム。
- 🔴 **夜遊び系のコンテンツ（キャバクラ・ニューハーフBAR等）は意図的に削除した。復活させない。** 「懇親会後の自由時間」というフローごと外し、茶寮のみグルメカードに集約してある。**公開URLなので資料性を重視する**という判断。
- ネタ元：Dropbox の `sendai-shiori-handoff.md` は**React作り直し案**で現行よりコンテンツが豊富（懇親会の多数決投票・フライト時刻表・持ち物リスト等）。**本番には未採用**で、現行HTMLに機能・情報を足す時の参考資料として見る。作り直しの指示ではない。

## 会場と交通（間違えやすい・確定済み）

- 2日目 全体会 = **仙台国際センター**（青葉山・**地下鉄東西線**）
- 1日目 分科会 = **3つの建物に分散**（2026-09-18 主催PDFで確定）：本町の江陽グランドホテル・パレスへいあん＋**青葉山の仙台国際センター**。**どの分科会がどこかは各分科会カードの📍が正**で、ここには書き写さない（2か所に書くとズレる）。⚠️ 以前ここに「1日目の分科会は本町の2会場」と書いていたが誤り。
- 1日目 懇親会 = 江陽グランドホテル（19:00〜）。国際センターの分科会組は移動が要る
- ⚠️ **広瀬通駅は南北線**。かつて「広瀬通なら3会場とも良好」と書いていたが**誤り**で、**仙台駅前が東西線に直行できて二日間とも最良**として推奨を再構成した。同じ誤りを繰り返さないこと。
- 「すべて徒歩圏内」も誤りで、繁華街に限った話として訂正済み。
- カウントダウンは**開催基準（10/1 13:00 開会）**。出発便基準ではない。

## 作業スタイル

- すべて JST。編集は `C:\dev\sendai-guide` のみ。母艦PC↔ノートPCで同時作業しない。
- 情報の正確さが価値の中心（参加者が実際にこれを見て移動・宿泊する）。**公式パンフレットの記載を正とし、推測で書かない。**

## 資料の所在

Dropbox `Claude Cowork/アプリ開発/【青全交】旅のしおりLP/`：`sendai-shiori-handoff.md`（React案・ネタ元）、`PDF資料/第54回青年経営者in宮城.pdf`（公式資料）、`2026年度_あいち報告会チラシ.pdf`、`PDF資料/青全交分科会会場一覧_20260918受領.pdf`（分科会の会場の出典）
