# 青全交のしおり in 仙台（sendai-guide）— Claude Code 起動時ガイド

> 毎セッション最初に読むブートストラップ。

## これは何

「中小企業家同友会 **青年経営者全国交流会（青全交）in 仙台**」（**2026/10/1(木)〜2(金)**）に参加する、名古屋青年同友会 第9支部の経営者 **約25名**向けのしおりWebサイト。幹事が情報をまとめて共有する用途で、**スマホ閲覧が主**。

- **公開URL：https://sendai.apppro-web.com/**（参加者に共有済みの公開サイト）
- 素の静的HTML。フレームワークなし・外部API/DBなし。
- ⚠️ **呼称は「青全交のしおり in 仙台」**（旧「旅のしおり」から変更済み）。「青全交」は公式パンフレットでも使われる正式略称。**古い資料に残る「旅のしおり」表記に引きずられないこと。**
- **期限のあるプロジェクト**。開催（2026/10/1）を過ぎたら役目を終える。申込締切は 9/16。

## デプロイ

- 配信：Cloudflare **Workers** プロジェクト `sendai-guide`（Static Assets）。**Pagesではない。**
- **`main` に push → Workers Builds が自動デプロイ**（数十秒）。
- `wrangler.jsonc`（`assets.directory = "."`）はリポジトリのルートにある。元はCloudflareが生成した `cloudflare/workers-autoconfig` ブランチにしか無かったが、mainに取り込み済みでビルドは正常。
- 運用ルール：**`main` は参加者が見る公開サイト**。文言・情報の微修正は main 直行可。レイアウトを触る改修は `preview/<topic>` → プレビューURLで確認 → main へ。
  - プレビューURL：`preview-<branch>-sendai-guide.apple-checker.workers.dev`（check-run の Preview Alias URL が正）
- ⚠️ **反映直後はエッジ／ブラウザのキャッシュで旧版が見えることがある。** 直したのに変わらない時は **Ctrl+Shift+R**。「デプロイが失敗した」と誤診しないこと。

## ファイル構成

- **`index.html`（約90KB）** … 本体。1ファイルにCSS/JS/データが全部入り。セクションは `#schedule` `#gourmet` `#access` `#hotel` `#area` の5つ。データはすべてHTMLにベタ書きなので、改修は該当箇所を直接編集する。
- **`sendai-report.html`（`/sendai-report`）** … 支部への報告資料（QRコード入り・別ページ）
- **`pamphlet.pdf`（`/pamphlet.pdf`）** … イベント公式パンフレット
- `qa/` … QA生成物。`.gitignore` で除外済み

## 改修時の方針（守ること）

- **現状のデザインを大きく崩さない**（ユーザー明言）。配色変数は `:root` の `--ink / --cream / --gold / --accent / --teal` で一元管理。Zen Old Mincho 明朝＋ゴールド×クリーム。
- 🔴 **夜遊び系のコンテンツ（キャバクラ・ニューハーフBAR等）は意図的に削除した。復活させない。** 「懇親会後の自由時間」というフローごと外し、茶寮のみグルメカードに集約してある。**公開URLなので資料性を重視する**という判断。
- ネタ元：Dropbox の `sendai-shiori-handoff.md` は**React作り直し案**で現行よりコンテンツが豊富（懇親会の多数決投票・フライト時刻表・持ち物リスト等）。**本番には未採用**で、現行HTMLに機能・情報を足す時の参考資料として見る。作り直しの指示ではない。

## 会場と交通（間違えやすい・確定済み）

- 2日目 全体会 = **仙台国際センター**（青葉山・**地下鉄東西線**）
- 1日目 分科会／懇親会 = 江陽グランドホテル・パレスへいあん（本町）
- ⚠️ **広瀬通駅は南北線**。かつて「広瀬通なら3会場とも良好」と書いていたが**誤り**で、**仙台駅前が東西線に直行できて二日間とも最良**として推奨を再構成した。同じ誤りを繰り返さないこと。
- 「すべて徒歩圏内」も誤りで、繁華街に限った話として訂正済み。
- カウントダウンは**開催基準（10/1 13:00 開会）**。出発便基準ではない。

## 作業スタイル

- すべて JST。編集は `C:\dev\sendai-guide` のみ。母艦PC↔ノートPCで同時作業しない。
- 情報の正確さが価値の中心（参加者が実際にこれを見て移動・宿泊する）。**公式パンフレットの記載を正とし、推測で書かない。**

## 資料の所在

Dropbox `Claude Cowork/アプリ開発/【青全交】旅のしおりLP/`：`sendai-shiori-handoff.md`（React案・ネタ元）、`PDF資料/第54回青年経営者in宮城.pdf`（公式資料）、`2026年度_あいち報告会チラシ.pdf`
